networkx>=3.0
fastapi>=0.110
uvicorn[standard]>=0.29
beautifulsoup4>=4.12
requests>=2.31
scikit-learn>=1.4
numpy>=1.26
scipy>=1.12
pydantic>=2.6
python-dotenv>=1.0
langgraph>=0.2
langchain-core>=0.3
streamlit>=1.33
anthropic>=0.34
pytest>=8.0

# Optional: enable dense semantic embeddings instead of TF-IDF
# (EMBEDDING_BACKEND=sentence-transformers)
# sentence-transformers>=2.6

# Optional: enable the real Neo4j-backed knowledge graph
# (GRAPH_BACKEND=neo4j)
# neo4j>=5.19

# Optional: enable the real PostgreSQL-backed long-term memory store
# (MEMORY_BACKEND=postgres)
# psycopg2-binary>=2.9
